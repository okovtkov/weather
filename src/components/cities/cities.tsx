const Cities = () => {
  return (
    <section className="cities">
      <h2 className="visually-hidden">Результаты сортировки</h2>
      <div className="cities__small-cards">*Small-Cards*</div>
    </section>
  );
};

export default Cities;
